import React from "react";

import Container from "../components/container";

export default () => (
    <Container>
        <h1>About CSS Modules</h1>
        <p>CSS Modules are cool</p>
        <pre>Look at this pre element inside of the custom container module</pre>
    </Container>
);
